# Use Python 3.10–3.12 on the server. Python 3.14 has no Pillow/torch wheels yet → broken source builds.
# Render: set env PYTHON_VERSION=3.12.8, or keep runtime.txt + nixpacks.toml at the service Root Directory.
fastapi==0.111.0
openai>=1.40.0,<2
uvicorn[standard]==0.29.0
python-multipart==0.0.9
# Pillow 10.3 has no wheel on Python 3.13+; source builds need libjpeg/zlib headers and often fail on Render/CI.
Pillow>=10.4.0,<12
numpy==1.26.4
easyocr==1.7.1
# Optional fallback when `tesseract` binary exists on the server (e.g. apt install tesseract-ocr)
pytesseract==0.3.10
